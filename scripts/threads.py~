import sys
import thread
import time
import Queue

def print_out ( str ):
	sys.stdout.write( str )
	sys.stdout.write( "\n" )
	sys.stdout.flush()

# Define a function for the thread
def print_time( queue ):
   count = 0
   while count < 5:
      time.sleep(delay)
      count += 1
      print_out (threadName)

# Create two threads as follows
try:
   thread.start_new_thread( print_time, ("Thread-1", 2, ) )
   thread.start_new_thread( print_time, ("Thread-2", 4, ) )
except:
   print "Error: unable to start thread"

while 1:
   pass


def main():

	c = Queue.Queue(maxsize=0)
	

if __name__ == '__main__':
    main()
